<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Contacts</h1>
    <ul>
    <% contacts.forEach( el => { %>
            <li class='contactItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.contact %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/contacts/createContact" method='POST'>
        <input type="text" placeholder="Enter Contact Item" name='contactItem'>
        <input type="text" placeholder="Enter Contact Item" name='contactItem'>
        <input type="text" placeholder="Enter Contact Item" name='contactItem'>
        <input type="text" placeholder="Enter Contact Item" name='contactItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>